[package]
name = "compat"
version = "0.0.1"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
chrono = "0.4.38"
polars = { version = "0.41.3", features = ["lazy", "parquet", "json", "cross_join"] }
